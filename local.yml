---
- hosts: workstation
  become: true 
  gather_facts: yes

  tasks:
    - name: Include Fedora Setup tasks if OS is Fedora
      include_playbook: Fedora-workstation/FedoraSetup.yml
      when: ansible_distribution == "Fedora"