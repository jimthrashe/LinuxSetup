---
- 
  
  vars_files:
    - /vars/vars.yml

  tasks:
    - name: Include Fedora repos setup
      include_tasks: tasks/fedorarepos.yml

    - name: Include packages setup
      include_tasks: tasks/packages.yml

    - name: Include users setup
      include_tasks: tasks/users.yml

    - name: Include services setup
      include_tasks: tasks/services.yml

    - name: Include system setup
      include_tasks: tasks/system.yml

    - name: Include third-party setup
      include_tasks: tasks/thirdparty.yml

    - name: Include hardening setup
      include_tasks: tasks/harden.yml

  handlers:
    - name: Import main handlers
      import_tasks: handlers/main.yml
